# Fish UI

This is a collection of Nuxt UI components. To use, just copy whichever component files you want to use into your components folder.

To customize the theme, start in the `variables.css` file and edit colors, shapes, and shadows there. This will affect all components. To make more specific changes, you will need to edit the components themselves.
